@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-gray-50 text-gray-900;
  }
}

@layer components {
  .ad-container {
    @apply my-4 overflow-hidden rounded-lg bg-gray-100;
  }
  .video-ad-overlay {
    @apply fixed inset-0 z-50 flex items-center justify-center bg-black/90;
  }
  .interstitial-overlay {
    @apply fixed inset-0 z-50 flex items-center justify-center bg-black/80 p-4;
  }
  .interstitial-content {
    @apply relative max-w-2xl rounded-lg bg-white p-6;
  }
  .market-ticker {
    @apply overflow-hidden whitespace-nowrap border-y border-gray-200 bg-gray-100;
  }
  .market-ticker-content {
    @apply inline-block animate-[ticker_30s_linear_infinite];
  }
  @keyframes ticker {
    0% { transform: translateX(100%); }
    100% { transform: translateX(-100%); }
  }
}